package ru.bmstu.labs

import org.junit.Test
import ru.bmstu.labs.kmeans.KMeansGUI
import kotlin.random.Random

class Lab4 {

    @Test
    fun kMeans() {
//        val n = 1000 // the number of data to cluster
//        val k = 10 // the number of cluster
//        val random = Random(System.currentTimeMillis())
//
//        val points = Array(n) { DoubleArray(2) }
//        // lets create random points between 0 and 100
//        for (i in 0 until n) {
//            points[i][0] = Math.abs(random.nextInt() % 100).toDouble()
//            points[i][1] = Math.abs(random.nextInt() % 100).toDouble()
//        }
//
//        val centroids = Array(k) { DoubleArray(2) }
//        // lets create random centroids between 0 and 100 (in the same space as our points)
//        for (i in 0 until k) {
//            centroids[i][0] = Math.abs(random.nextInt() % 100).toDouble()
//            centroids[i][1] = Math.abs(random.nextInt() % 100).toDouble()
//        }
    }
}